
export const makeCalendar = function () {
  
}


const startYear = 2018;
const endYear = 2050; 

function calendarDays () {
  //first day of month
  let firstDay = (new Date(year, month)).getDay();
 
}

function daysInMonth (year, month) {
  //days in month
    return (32 - newDate(year, month, 32).getDate());
}

